# Night of 2026-08-19 — R8, the probe, Slice A, two screens

**Nothing merged. Three PRs open, two decision screens waiting, three R8 falsifiers still unmeasured.**

---

## What was ruled

**R8 — the channel is an in-process HTTP host inside Gramps.** Recorded verbatim in substance at
`docs/rulings/R8-channel-architecture.md`, with the roadmap and README falsified against it row by row.

**Six disputes came back from the recording and the conductor ruled all six** under the owner's standing
instruction to decide rather than wait:

| | Ruling |
| --- | --- |
| Record the probe in R8's Falsifiers | ✅ **Yes** — the ruling understated what was known |
| *Transport returns, four-route API does not* | ✅ **Confirmed** — matches the owner's own words |
| Gramplet returned to its use-derived condition | ✅ **Confirmed** — R2 un-parked it to host the channel; R8 rules it cannot, so the job is gone |
| `docs/using.md`'s *"the lock is the one-writer rail"* | ⚠️ **False under R8 — fixed.** Policy kept, reason corrected |
| Close #66 as dissolved | ⛔ **Declined.** Its defect is live today; the spawned-CLI writer still exists. Commented with the dissolution condition instead |
| Narrow R2's undo benefit rather than delete it | ✅ **Confirmed** |

⚠️ **The declined one is the one worth re-checking.** Closing an issue whose defect is still live is
falsifying the record. #66 closes **when the spawned-CLI writer is deleted** — not when R8 was ruled.

---

## What the probe returned

**PASS.** `load_on_reg` fired inside the AIO's own interpreter via the Gramps CLI (`-L`, no tree opened):

```
python=3.14.4 (main, Apr 11 2026) [MINGW GCC 15.2.0 64 bit (AMD64)]
uistate=False
http.server=OK  socketserver=OK  socket=OK  ssl=OK
json=OK  secrets=OK  threading=OK  hmac=OK  GLib=OK
```

**No `lock` file was created** — verified against a baseline taken before the run. Probe directory
removed.

**⛔ Two falsifiers discharged:** `load_on_reg` does not fire on the AIO · `http.server`/`socket` absent.

⚠️ **Three still open, and they need a GUI session with a tree loaded:**
`GLib.idle_add` under a modal nested main loop · a `DbTxn` write refreshing the Gramps UI · the ~2 s
round trip. **The probe was a CLI run and says nothing about any of them.**

**Two findings the ruling did not anticipate:**

1. ⚠️ **The AIO ships Python 3.14.4** while this repo targets `py310` and CI runs 3.10–3.12. **Source
   must be 3.10-compatible and executes on 3.14** — so a behaviour that moved between 3.12 and 3.14 is
   **invisible to every gate here.** Not a constraint to follow; a hole in the safety net.
2. **`uistate=None`, no tree open** — R8 predicted the `DummyDb` condition; now measured.

---

## Did Slice A demo?

**No — it is built and gated, not demoed.** The demo requires Gramps running on the owner's machine.
**[PR #82](https://github.com/randyjreid/gramps-live-api/pull/82)**, 4 commits, 23 files, +2,783 lines,
64 new tests, **1490 passed / 6 skipped**, guard 0 findings, lock files unchanged before and after.

**The demo to run** is in `docs/slice-a-demo.md`: start Gramps, then `/health` with a valid token → the
tree's name and person count · close the tree → clean *no tree open* · bad token → 401 · `Origin` header
→ 403.

**⭐ The real deliverable is the boundary, enforced by tests that name nothing.** Rule 1 parses
`accessor.py` for every top-level public `def` and requires each to refuse a non-main thread — **so the
fourth helper is covered by the act of writing it.** A third test guards that premise and **fired on its
own subject** (`Any` and `on_main_thread` were reachable and invisible to a `def` scan), fixed **with no
exemption list**, because an exemption for an obviously-harmless callable is the enumeration failure one
level up. Rule 2 is an AST walk refusing `.db` outside the accessor, **with its bound stated: it is
syntactic, and `getattr(dbstate, "db")` escapes it.**

**One dispute worth the owner's eye:** the Windows ACL on the token file **buys close to nothing against
R8's own threat model** — `%APPDATA%` already grants only the user, SYSTEM and Administrators, and R8's
accepted residual already concedes a process running as the owner. What it **costs** is a subprocess
spawn (`icacls`, since the AIO has no pywin32) **on Gramps' startup path**, which is where an AV scanner
intercepting process creation shows up. Implemented as ruled, capped at 10 s, non-fatal, achieved
protection logged as fact. **The builder would drop it; the conductor left it in place rather than
reverse a security control unilaterally.**

---

## Waiting on Randy

| | |
| --- | --- |
| **[#82](https://github.com/randyjreid/gramps-live-api/pull/82)** | Slice A — ⚠️ **stacked**, contains #81 and R8. Merging it merges those |
| **[#81](https://github.com/randyjreid/gramps-live-api/pull/81)** | R2's roadmap record — redundant if #82 merges |
| **[#80](https://github.com/randyjreid/gramps-live-api/pull/80)** | #77's message fix |
| **Slice A's demo** | Only the owner's machine can run it |
| **R3** | `.claude/decisions/R3-injection-under-live-reads.md` — recommends **D + A** on a tested property |
| **R7** | `.claude/decisions/R7-undo-and-backup-with-gramps-open.md` — recommends **1 now, 3 via the batch slice, 2 only on a measurement** |
| **The three open falsifiers** | Need a GUI session; **owed before more is built on the host** |

---

## Every error the conductor made tonight

1. ⚠️ **The R2 commit was never pushed**, so PR #79 merged without it and `main` lacked the ruling.
   Recovered as #81. **The gap: *"do not push"* is right for a build and is not a disposition — a commit
   on a branch with an open PR needs the conductor to push it deliberately.**
2. **A heredoc broke on quoting** when posting the morning report; rewritten to a file and posted. No
   content lost, one wasted call.
3. **R8's first dispatch died on a lost connection** after 9 turns / $1.24. Nothing had landed, so it was
   re-dispatched rather than resumed — verified before deciding.
4. ⚠️ **The R8 brief told the builder the probe had not returned when it had.** The builder caught it,
   made no false claim in either direction, and flagged it. **A brief written from a stale snapshot of my
   own state.**
5. **The result JSON reported `"subtype": "success"` on a failed run** — third time this session.
   Recorded again because it keeps being tempting: `is_error` and the `result` text are the only fields
   that mean anything.

**Costs tonight:** roughly **$32** across seven dispatches (R8 ×2, follow-up, Slice A, plus the earlier
run's tail).

---

# The GUI session — the three falsifiers, measured

**The heading above says *"three R8 falsifiers still unmeasured."* That is no longer true.** A real
Gramps AIO 6.0.8 GUI session ran at **08:52:56–08:53:09** against the **blessed copy**
(`RandyReid-Testing`, `…\grampsdb\6a821852`, carrying `.gramps-live-api-copy`). Raw logs:
`%APPDATA%\gramps-live-api\falsifiers.txt` and `insitu.txt`.

**Two of the three are discharged. One is partially measured and stays open.**

## Falsifier by falsifier

| | Measured? | The number | What it means for R8 |
| --- | --- | --- | --- |
| **F1** — `idle_add` does not run under a modal nested loop | ⛔ **YES — discharged** | dialog held the main thread **3.004 s**; callback scheduled from `f1-worker` **fired 0.0002 s later**, dialog **still up**; main-loop depth **1 → 2** | ⭐ **R8's approval surface survives.** Approval is a Gramps dialog on the main thread driven from an HTTP request — this is the falsifier that could have killed it |
| **F2** — a `DbTxn` write from `idle_add` does not refresh the UI | ⚠️ **PARTIALLY** | crossed in **0.0002 s**, ran on **`MainThread`**, committed in **0.0709 s**, notes **60 → 61**, read back **True**, on the person's note list **True**; `note-add` and `person-update` each **1 handle, ours, on `MainThread`** | **The write and the mechanism are confirmed. The visible outcome is not.** ⛔ **Not discharged** |
| **F3** — a read round trip exceeds ~2 s | ⛔ **YES — discharged** | **25 iterations:** median **2.2 ms**, min **1.5 ms**, p90 **3.4 ms**, worst **89.9 ms** (first call, cold) | **Budget ~2000 ms.** Worst case **~22× inside it**; median **~900× inside it**. Room to spare |

### ⚠️ Why F2 is only half an answer

The log's F2 verdict reads **`refreshed in place: NOTHING`**, and that line is easy to misread as *the
UI does not refresh.* It is not that.

**The active page was `DashboardView` before and after the write** — a dashboard displays neither
notes nor people. **The probe never put a view that cares about the written objects in front of the
write.** So the mechanism that drives refresh is confirmed firing (both signals, both carrying our
handle, both on `MainThread`, already heard by the time the transaction closed and unchanged 1.5 s
later) while the thing the falsifier actually asks about was never exercised.

**What settles it:** the same measurement with a **Person or Note view active**.

### ⚠️ What F3's demo did not do

**Demo step 2 — "close the tree in Gramps, and ask again" — was NOT performed.** It needs a GUI menu
action and nobody was there to click it. **Recorded as unperformed, not as passed.** The *code path*
was reached from the other side: `host.log` carries
`2026-08-19T08:52:58 INFO database-changed: no tree open` at startup, before the tree opened.

Also measured in the same call: **bad token → 401**, **`Origin` header → 403**, and the tree answer
`{ok: true, tree: {open: true, name: "RandyReid-Testing", people: 2924}}`.

## The in-situ 3.14 run — the gap the ruling named, measured once

The night's finding 1 was that the AIO ships **3.14.4** while CI runs 3.10–3.12, so the gap is
untested **by construction**. It still is, for the gates. But the suite was run **once, in situ**,
inside the AIO's own frozen interpreter:

- **Route 1 (`pytest`) unavailable** — not importable; **the AIO has no `site-packages` at all**.
- **Route 2 (`unittest`) collected 0** from the five modules that imported — plain `test_*` functions
  with bare asserts, and `TestLoader` finds only `TestCase` subclasses.
- **Route 3 taken** — the real test functions called directly behind a minimal `pytest` stand-in.

**Result on 3.14.4: 60 passed, 0 failed, 9 skipped, 0 module errors.** The same harness on the
project's **3.12: identical 60 / 0 / 9.** ⭐ **No 3.12 → 3.14 divergence in the covered surface.**

- **`tests/unit/test_host_thread_boundary.py` — the load-bearing suite — ran 8 of 8, all passing,
  under 3.14.**
- ⚠️ **`sys.flags.optimize` was checked and is 0.** Under `-O` every bare assert is stripped and the
  whole suite would pass **vacuously**. The check is what stops the result being meaningless.
- **Negative-controlled:** a module with a false assert, a `raises` that does not raise, and a
  `raises` with a wrong `match` reported **1 passed, 3 failed**, with tracebacks.
- **9 skipped, by name** — those needing `monkeypatch` or parameters the stand-in does not supply.
  **Nine, not "a few".**

⛔ **This is not CI coverage of 3.14.** It is a separate in-situ run on one machine, not a gate.

## The lock-file answer

**Baseline before launch: `RandyReid` lock=False, `RandyReid-Testing` lock=False.** After a graceful
close (`CloseMainWindow`, **exited cleanly in 10 s**): **both still lock=False.** **No lock file
exists that did not exist at baseline. Gramps was never force-killed.**

## ⚠️ An artifact was written to the owner's copy tree

> **The F2 measurement wrote note `N0060` onto person `I0001` in the blessed copy
> (`RandyReid-Testing`).** It is a measurement artifact and **should be deleted by hand**.
>
> **The conductor did not delete it.** Deleting is a destructive operation, and **#25 defers those
> until a backup is proven.**

The log says so itself, twice, in the words the probe chose:
`⭐ ARTIFACT WRITTEN: note N0060 on person I0001 -- delete this note when done` and
`⭐ REMEMBER: delete note N0060 from person I0001 in the blessed copy tree.`

## What is now owed

| | |
| --- | --- |
| **F2** | Re-measure with a **Person or Note view active**. Until then R8 claims the mechanism, not the refresh |
| **Demo step 2** | Close the tree in Gramps and call again — one menu click, still unperformed |
| **Note `N0060`** | Delete by hand from `RandyReid-Testing`, person `I0001` |
| **3.14** | Still ungated. The in-situ run is evidence, not a safety net |
